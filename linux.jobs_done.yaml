junit_patterns:
- "ben10/pytest.xml"

build_shell_command: "$AA_AASIMAR_AA -t project.build ben10 --projects-dir=$WORKSPACE --dist={dist} --platform={platform}"

description_regex: "HUDSON DESCRIPTION: (.*)"

dist:
- 12.0

platform:
- redhat64
